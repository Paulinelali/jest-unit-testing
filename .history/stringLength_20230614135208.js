


const stringLengthCount = (str) => {
    if(str.length < 1 && str.length > 10){

        
        
        return "Not allowed"
    }else{ 
    return str.length}
}

module.exports = stringLengthCount